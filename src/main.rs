use packrat::model::task::{Task, TaskManager};

    
fn main() {
   // let task = Task::new(String::from("clean"), String::from("do the dishes"));
   // assert_eq!(task.name, "clean");
   // assert_eq!(task.desc, "do the dishes");
   //  
   // let ser_task = task.to_bytes();
   // println!("Task: {:?}", String::from_utf8(ser_task.clone()));
   // // assert_eq(String::from_utf8_lossy(ser_task), "clean");
    println!("Hello, world!");


}
